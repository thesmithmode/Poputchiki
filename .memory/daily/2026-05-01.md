# Daily Log: 2026-05-01

## Sessions

## Memory Maintenance

### Memory Flush (14:08)

FLUSH_OK - Nothing worth saving from this session

### Session (14:56)

**Context:** Масштабная ревизия проекта Poputchiki — удаление Supabase, переход на self-hosted, расширение tasks.json до 125 задач.

**Key Exchanges:**
- Полная замена Supabase на self-hosted PostgreSQL 16 в Docker во всех docs (SPEC, PRD, OPEN-QUESTIONS, CLAUDE.md, AUTOMATION.md)
- RLS переведён с `auth.uid()`/`auth.jwt()` на `app.current_user_id()` GUC + `withIdentity()` helper
- tasks.json расширен с 106 до 125 задач (+8 missing core MVP + 11 prod-deploy)
- Phase переименован: `code-only-local` → `full-mvp-with-prod-deploy`

**Decisions Made:**
- Self-hosted everything: Postgres, Nominatim, observability, Sentry — запрет managed-сервисов
- RLS identity через SQL GUC `set_config('app.current_user_id', ...)` в начале каждой транзакции
- Deploy: GHA → GHCR → SSH → docker compose, домен `poputchiki.searchingforgamesforever.online`
- Priority bumps: TASK-052 low→high, TASK-062 low→critical, TASK-061 medium→high

**Lessons Learned:**
- bash ломает кириллицу в commit messages → PowerShell heredoc
- `auth.uid()` оставлен только в warning-строках ("НЕ использовать") — намеренно
- JSON validation после массовых правок tasks.json — обязательна (125 задач, легко сломать)

**Action Items:**
- Запуск автономного loop с TASK-001 (Bun monorepo + Biome)
- Plan B задокументированы в OPEN-QUESTIONS для: iOS cookie SameSite, SSE нагрузка, TG SDK quirks

