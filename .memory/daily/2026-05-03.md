# Daily Log: 2026-05-03

## Sessions

## Memory Maintenance

### Memory Flush (08:02)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (11:54)

**Context:** Ревью ветки `feat/auto-dev` перед squash merge в `dev` — проверка авторства коммитов, соответствия плану, качества кода, и исправление найденных багов.

**Key Exchanges:**
- Антон остановил merge до ревью: все 25 коммитов от `Claude <noreply@anthropic.com>` → нельзя в `dev`
- Проведено code review через superpowers:code-reviewer субагент
- Найдено 2 критических + 3 важных + 4 минорных проблемы
- Антон попросил чинить баги самостоятельно и сверяться с планом

**Decisions Made:**
- Squash merge всего в один коммит с автором `thesmithmode` (не сохраняем историю отдельных задач)
- Фиксы багов делаются прямо в той же ветке перед merge
- Claude-соавтор (`Co-Authored-By:`) полностью запрещён

**Lessons Learned:**
- Все субагенты коммитили от `Claude <noreply@anthropic.com>` — нужен механизм принудительного `GIT_AUTHOR_NAME`/`GIT_COMMITTER_NAME` для субагентов
- Критические баги найдены: XFF-спуфинг обходит rate-limit; race condition в idempotency middleware
- Deleted_at не проверяется в `/auth/refresh`; logout не ревокирует access jti

**Action Items:**
- Завершить squash merge ветки в `dev` от имени `thesmithmode`, без Co-Authored-By
- Убедиться CI зелёный после merge
- Добавить механизм контроля git author для субагентов (issue или CLAUDE.md правило)
- Найденные важные issues (#3-#5) — создать задачи в tasks.json если не покрыты

### Session (12:12)

**Context:** Чистка git-веток после squash merge, обновление правил CLAUDE.md, сжатие файлов без потери смысла

**Key Exchanges:**
- Выяснили: `review-fixes` ветка содержала те же изменения что уже включены в squash-коммит `6b46d2f` → cherry-pick не нужен, ветки удалены
- claude.ai/code URL проскальзывали в commit body через автоматическое поведение Claude Code (правило было в CLAUDE.md, но игнорировалось)
- Нейросети `**жирный**`/`---`/пустые строки между пунктами не нужны — только структурные маркеры `##` и ` ``` `

**Decisions Made:**
- Не переписывать историю `dev` от claude.ai URL — деструктивно, SHA сломает `main` и CI
- `## Стек напоминание` в локальном CLAUDE.md не сжимать — важен как reference
- Дублирование правил между глобальным и локальным CLAUDE.md — намеренно, т.к. работа с разных ПК без глобального конфига
- Метод сжатия: убирать только декор (`**`, `---`, лишние пустые строки, дубликаты разделов) — без потери смысла

**Lessons Learned:**
- Правило запрета AI-подписей было прописано, но Claude Code всё равно добавлял claude.ai URL в commit body — правила нужно формулировать с явными URL-паттернами
- Глобальный CLAUDE.md уже был плотным — сжатие дало только -5 строк; локальный дал -33

**Action Items:**
- Локальный CLAUDE.md: 233 → 200 строк (сделано, не закоммичено — уточнить)
- Глобальный CLAUDE.md: обновлён, добавлено правило про URL-паттерны и формат файла
- Проверить что коммит изменений локального CLAUDE.md сделан

### Memory Flush (12:21)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (12:38)

FLUSH_OK - Nothing worth saving from this session

### Session (12:44)

**Context:** Продолжение разработки Poputchiki (TASK-022) + исправление настроек auto-compact в Claude Code

**Key Exchanges:**
- TASK-022 (materialized view `user_stats` + cron refresh) завершена
- Выяснили что `autoCompactWindow` top-level ключ в settings.json не работает — недокументирован и игнорируется Claude Code

**Decisions Made:**
- Перенесли порог компакта из top-level в `env.CLAUDE_CODE_AUTO_COMPACT_WINDOW=150000` — единственный официальный способ
- Auto-compact срабатывает mid-turn (сразу по порогу), не ждёт конца задачи

**Lessons Learned:**
- `autoCompactWindow` и `autoCompactEnabled` как top-level ключи в settings.json — не из официальной схемы. Работает только env-переменная `CLAUDE_CODE_AUTO_COMPACT_WINDOW`
- Env-переменные читаются на старте сессии → изменения подхватываются только после рестарта

**Action Items:**
- TASK-114 (sentinel migrations + RLS identity leak) — следующая задача, была прервана
- Проверить после рестарта что компакт реально срабатывает на ~150k токенов

