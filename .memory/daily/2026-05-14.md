# Daily Log: 2026-05-14

## Sessions

## Memory Maintenance

### Session (12:58)

**Context:** Фронтенд Poputchiki — auth flow, token store, apiFetch, HashRouter fix

**Key Exchanges:**
- Telegram Desktop добавляет `#tgWebAppData=...` к URL → конфликт с HashRouter → решение: cleanup hash в `main.tsx` до монтажа React
- `/api` префикс: вместо правки 12+ callers — добавили автопрефикс в `apiFetch` для всего кроме `/auth/*`
- `ImportMetaEnv` — Biome не понимает стандартный Vite pattern → добавлен `biome-ignore`

**Decisions Made:**
- `apiFetch` автоматически добавляет `/api` префикс для non-auth путей (централизованно, не в каждом caller)
- logout передаёт токены в body + вызывает `clearTokens()`
- `useMe` делает полный auth flow: `POST /auth/telegram` с initData → save tokens → `GET /users/me`

**Lessons Learned:**
- Тест `SettingsScreen.test.tsx` проверял точный аргумент `{ method: "POST" }` — сломался когда добавили `body` в logout. Sentinel-тест нужно обновлять при изменении контракта вызова
- CI упал именно из-за этого — тест не покрывал новый body logout

**Action Items:**
- Дофиксить `SettingsScreen.test.tsx` (тест logout — добавить проверку body с токенами)
- CI должен позеленеть после фикса теста

### Memory Flush (13:18)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (13:24)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (13:28)

FLUSH_OK - Nothing worth saving from this session

