# Daily Log: 2026-05-16

## Sessions

## Memory Maintenance

### Session (11:31)

**Context:** Poputchiki — фикс 4 задач: auth security, geocoding bbox, пресеты Царёво, карта маршрута

**Key Exchanges:**
- Пользователь указал: auth-баг (User B видел профиль User A) — критическая ошибка безопасности, требует /attack-review с параллельными субагентами
- Пресеты Царёво реализованы как кнопки — пользователь не принял: нужен выпадающий список, не кнопки; реализация под вопросом, нужно обсуждение
- Карта маршрута (RouteMapLeaflet) — UI баг со смешением цветов не был устранён, задача считается незакрытой

**Decisions Made:**
- Auth fix: `useMe.ts` сравнивает `JWT.sub` с `Telegram.initDataUnsafe.user.id`, несовпадение → `clearTokens()` → re-auth
- Geocoding bbox: исправлен формат `BBOX_KAZAN_AREA` (`left,top,right,bottom`), `bounded=1` — только Казань+Царёво
- Пресеты — подход с кнопками отклонён, нужно переделать в dropdown/autocomplete-список

**Lessons Learned:**
- Auth критика: JWT.sub vs Telegram user ID несравнение — security gap класса account takeover; нужен систематический /attack-review
- Пресеты в AddressAutocomplete: UX-паттерн "кнопки внизу поля" ≠ "выпадающий список при наборе" — разные ожидания
- Карта: placeholder-замена без фикса видимого UI-бага — задача не закрыта

**Action Items:**
- Провести /attack-review с 3-5 параллельными субагентами по auth-слою
- Обсудить с пользователем дизайн пресетов Царёво: dropdown в autocomplete vs отдельный UI элемент
- Зафиксировать и закрыть UI-баг карты (смешение цветов, карта не работала) — смотреть скрин

### Memory Flush (12:08)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

