# Daily Log: 2026-05-17

## Sessions

## Memory Maintenance

### Memory Flush (11:35)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (11:48)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (12:03)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (12:12)

**Context:** Отладка Poputchiki — 4 бага/фичи: тип поездки, flow записи пассажира, seats_taken логика, UI-переименование

**Key Exchanges:**
- Юзер: регулярная поездка отображается как "Разовая", нет возможности записаться, места должны уменьшаться только после подтверждения водителем, "В Telegram" → "Связаться с водителем"
- Advisor подтвердил план: начать с быстрых UI-правок, затем TDD для flow

**Decisions Made:**
- `book_seat()` убрать из `POST /:id/request` — вызывать только при `PATCH /:id/requests/:reqId` (accept)
- Добавить эндпоинт `PATCH /rides/:id/requests/:reqId` (accept/reject) — отсутствует полностью
- "Откликнуться" → "Записаться", pending_requests отдавать водителю в `GET /:id`
- Если `is_recurring === true` в CreateRideScreen → `POST /ride-templates` вместо `POST /rides`

**Lessons Learned:**
- Root cause "Разовая": `ride.template_id` nullable, в RideDetailScreen захардкожено "Разовая" без проверки; `CreateRideInput` не имеет template-полей; нет эндпоинта `POST /ride-templates`
- Root cause broken join: `book_seat()` срабатывает на request, не на accept → `seats_taken` не отражает реальность; `GET /mine?role=passenger` фильтрует `status='accepted'` который никогда не достигается
- Тесты `markParticipants.test.ts`, `confirmParticipation.test.ts` используют `POST /request` для setup — нужно обновить при рефакторинге

**Action Items:**
- [x] Task 1: "В Telegram" → "Связаться с водителем"
- [x] Task 2: `template_id` в `Ride` type + динамический лейбл
- [ ] Task 3 (TDD): рефакторинг request flow + driver UI Принять/Отклонить
- [ ] Task 4 (TDD): `POST /ride-templates` + `CreateTemplateInput` schema + routing в CreateRideScreen

### Memory Flush (12:40)

FLUSH_OK - Nothing worth saving from this session

### Session (13:12)

Деплой зелёный. Всё в проде.

### Session (13:38)

**Context:** Фикс 9 багов Poputchiki — геолокация, уведомления водителю, lazy loading экранов

**Key Exchanges:**
- EventsScreen была полной заглушкой — backend не имел GET endpoint для уведомлений
- pg_notify использовался для realtime, но уведомления не персистились в БД

**Decisions Made:**
- Добавлена миграция `025_user_notifications.sql` — таблица `user_notifications`
- INSERT уведомления в ridesRouter при бронировании + в rideRequestsRouter при accept/reject
- GET `/notifications` endpoint добавлен в notificationsRouter
- Тяжёлые экраны (MapScreen, EventsScreen и др.) конвертированы в `React.lazy` для lazy loading
- Геолокация: добавлен fallback через Telegram LocationManager + error feedback в UI

**Lessons Learned:**
- `result.rideRequest` из транзакции нужна explicit undefined guard — TS не прощает
- Biome autofix справляется с большинством lint ошибок автоматически

**Action Items:**
- Проверить CI после push всех 9 фиксов
- Миграция 025 нужна на prod при деплое

### Memory Flush (14:04)

FLUSH_OK - Nothing worth saving from this session

### Session (14:04)

**Context:** Исправление красного CI после добавления fire-and-forget INSERT в ridesRouter, squash merge dev→main, deploy в prod.

**Key Exchanges:**
- CI упал на 4 тестах после предыдущих изменений: `CreateRideScreen` (старые data-testid кнопок мест), `RideDetailScreen` (telegram link → button, отсутствие мока useMe), `App.test.tsx` (дефолт темы `"light"` вместо `"system"`)
- `test:unit` не запускает web тесты — только `test:web` покрывает фронт

**Decisions Made:**
- Squash merge dev→main автоматически после зелёного CI (согласно правилам проекта)
- При конфликтах merge брать версию dev как источник истины

**Lessons Learned:**
- Паттерн: добавил новую `mockSql`-операцию → сразу обновлять mock в смежном тест-файле (`mockResolvedValueOnce`). Fire-and-forget INSERT = дополнительный resolved mock в тесте. Не ждать падения CI.
- localStorage ключ темы: реальный `"pp_theme"`, не `"themePreference"`. Тесты должны использовать тот же ключ что приложение — брать из константы, не хардкодить
- `applyTelegramTheme` в `beforeEach` нужно мокировать чтобы убрать side-effect на document.documentElement

**Action Items:**
- Deploy в prod запущен — дождаться результата и доложить

### Session (15:04)

**Context:** Серия UI/UX и backend фиксов для Poputchiki — нотификации, лента, бейджи, контакт-кнопка

**Key Exchanges:**
- `pg_notify` был хаос: разные имена каналов (`ride_request`, `ride_cancelled`, `participation_request`). Стандартизировали всё на `notify_user` + обязательное поле `user_id`
- Лайк-кнопка возвращала 403 `not_confirmed` для незавершённых поездок — нужно показывать понятное сообщение, не крашить

**Decisions Made:**
- Единый канал `notify_user` для всех пользовательских уведомлений — семантически правильнее чем per-event каналы
- "Ваша поездка" badge + левый border в RideCard для собственных поездок водителя в ленте (isOwnRide через useMe)
- Кнопка "Связаться" через `tg_username` из driver объекта (добавлен в rides query)
- Unread count через отдельный hook `useUnreadCount` → badge в BottomTabBar

**Lessons Learned:**
- Был на `main` при попытке коммита — stash → checkout dev → unstash. Напоминание: всегда проверять ветку перед коммитом

**Action Items:**
- Убедиться что CI зелёный в `dev` после пуша batch-фиксов
- Проверить что `notify_user` channel корректно слушается в notifier сервисе

