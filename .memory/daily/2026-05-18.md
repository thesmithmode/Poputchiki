# Daily Log: 2026-05-18

## Sessions

## Memory Maintenance

### Session (14:33)

**Context:** Системный аудит и ремонт подсистемы уведомлений в Poputchiki (Telegram MiniApp)

**Key Exchanges:**
- Полный Phase 1 аудит уведомлений выявил 11 root causes — от missing single-mark endpoint до pg_notify без INSERT в user_notifications
- Advisor верифицировал план, рекомендовал ship B1+B2 быстро, отложить SSE, ввести `enqueueNotification` helper
- Юзер добавил scope: badge sync, дублирование в TG бот, inline buttons "Принять/Отклонить" с callback_query
- Юзер жёстко напомнил: НИКАКИХ feature branches, НИКАКИХ PR — только dev + merge

**Decisions Made:**
- Удалена мусорная ветка `fix/notifications-overhaul`, работа вернулась в dev
- Декомпозиция на 5 логических блоков (не PR): single-mark endpoint, архитектура enqueue, UI redesign, TG inline, TG re-enable
- B1 (single-mark read endpoint) + B2 (typo `notify_user` → `ride_request` в category) — стартовали первыми
- `UUID_RE` regex добавлен в shared пакет

**Lessons Learned:**
- 8 из 11 `pg_notify` call sites не пишут в `user_notifications` → in-app feed пропускает большинство событий
- TG bot 403 при первом sendMessage юзеру без `/start` ставит `notify_disabled=true` навсегда — вероятная главная причина "бот не шлёт"
- likes/reviews/favorites роутеры вообще не emit'ят pg_notify — функционал отсутствует целиком
- Category drift: 4 разных списка categories в разных файлах, notifier reject'ит некоторые валидные

**Action Items:**
- B1 backend (POST `/notifications/:id/read`) — код написан, 754 теста green, коммит прерван юзером
- B2 sentinel test написан (typo fix `notify_user` → `ride_request`)
- Остаток B1: frontend handler `handleNotificationClick` → mark read before navigate
- B2-B5: enqueueNotification helper, закрытие 8 дыр, like/review/favorite emit, EventsScreen UI, TG inline buttons, /start re-enable flow
- dev отстаёт от main на 6 коммитов — синхронизация выполнена (merge main → dev)

### Session (15:01)

**Context:** Poputchiki MVP — серия задач из tasks.json (#13–#16), TDD-цикл

**Key Exchanges:**
- Задача #13: TDD цикл, 5/5 тестов green, полный сьют 754 API + 434 web — push
- Задача #14: shared canonical CATEGORIES + enqueueNotification helper, 766/766 green — push
- Задача #15: замена 11 pg_notify call sites на enqueueNotification, перенос helper из api в shared package
- Задача #16: начата (emit like_received / review_received / favorite_new_ride) — прервана

**Decisions Made:**
- enqueueNotification перенесён из `apps/api` в `packages/shared` — нужен доступ из cron + api
- Импорты api перенаправлены на shared, api-only копия удалена

**Lessons Learned:**
- `bun test` использует bun-runner, не vitest — нужно явно запускать vitest
- При замене pg_notify → enqueueNotification порядок mock вызовов меняется (INSERT первый, pg_notify второй) — тесты нужно обновлять

**Action Items:**
- Завершить задачу #16: emit like_received / review_received / favorite_new_ride (sentinel-тесты добавлялись когда прервали)

### Memory Flush (15:27)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (15:52)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

