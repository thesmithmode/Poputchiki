**User:** Анализ: Explicit DTO layer перед отдачей ответа. Паттерн service → raw DB row → DTO transform → response стоит проверить у нас: нет ли мест, где мы отдаём объект из БД напрямую без фильтрации полей. При наличии pgcrypto-полей это критично — случайно можно отдать зашифрованный blob вместо пустого поля.  Проблема есть? Её следует решить?

**Assistant:** Хороший вопрос — особенно с учётом pgcrypto-полей. Запускаю параллельное исследование кодовой базы.

**Assistant:** Два из трёх агентов вернулись с результатами. Пока ждём третьего, прочитаю ключевые файлы для верификации.

**Assistant:** Теперь посмотрю роутеры, которые возвращают данные напрямую.

**Assistant:** Посмотрю оставшиеся роутеры и таблицу rides для проверки, какие столбцы есть в `rides`.

**Assistant:** Проверю, не добавлялись ли чувствительные столбцы в `users` в последующих миграциях.

**Assistant:** I now have a comprehensive picture. Let me launch a Plan agent to design the solution.

**Assistant:** Both exploration agents have completed. I have a thorough picture of the codebase. Let me write the plan based on all findings while the design agent finishes.

**Assistant:** Всё общение веду на русском.

Вот краткий итог исследования:

**Прямой утечки pgcrypto-полей сейчас НЕТ.** Все запросы к таблице `users` явно перечисляют столбцы и никогда не выбирают `phone_enc`/`apt_number_enc`. Тесты это проверяют (`users-me.test.ts:111-127`).

**Но архитектурная хрупкость есть:**
- `ridesRouter.ts` в 7 местах использует `SELECT r.*` и `RETURNING *` — сегодня безопасно (в rides нет PII), но любой новый столбец автоматически утечёт в ответ
- Zod-схемы `UserDTO`, `RideDTO` в `packages/shared/` написаны, но **нигде не используются** для фильтрации ответов — мёртвый код
- Два несогласованных паттерна: `usersRouter` имеет `shapeMe()`/`shapePublic()`, остальные роутеры возвращают сырые строки из БД

**План исправления** — двойной барьер:
1. Заменить `SELECT r.*` на explicit column lists в SQL
2. Активировать существующие DTO-схемы как strip-функции перед `c.json()`
3. Добавить тесты на отсутствие лишних полей в ответах
