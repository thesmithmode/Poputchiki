{
  "name": "@poputchiki/notifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "echo 'notifier: no sources yet, skip typecheck'",
    "build": "echo 'notifier: no build step (bun runtime)'"
  }
}
