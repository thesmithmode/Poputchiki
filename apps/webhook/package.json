{
  "name": "@poputchiki/webhook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "echo 'webhook: no sources yet, skip typecheck'",
    "build": "echo 'webhook: no build step (bun runtime)'"
  }
}
